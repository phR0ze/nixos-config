# keyd

Kernel-level key remapper. Maps input device keys to a virtual output device.

## Configuration Notes

- Configured for device ID `1915:1025` (Pepper Jobs remote)
- Key mappings defined in `default.nix` under `keyboards.default.settings`
- Useful for mapping specialty/media keys to standard shortcuts handled by XFCE keyboard shortcuts
