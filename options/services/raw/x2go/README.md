# x2go

X2Go remote desktop server. Provides full remote desktop sessions over SSH.

## Configuration Notes

- Listens on TCP port 5900
- Debug client connections: `x2goclient --debug 1> /tmp/x2goclient.stdout.log 2> /tmp/x2goclient.stderr.log`
