# Synology Drive Client

Synology NAS sync client. Optionally autostarts after login.

## Configuration Notes

- `autostart = true` (default) writes an XDG autostart desktop entry
- Launched with `sudo` as it requires elevated privileges
